# Variáveis de ambiente para Docker Compose

# IP do ESP32-CAM
ESP32_CAM_IP=192.168.100.56

# Porta do servidor HTTP
PORT=3000

# Porta do WebSocket
WS_PORT=8080

# Ambiente (production/development)
NODE_ENV=production

# Modo de captura (capture ou stream)
CAPTURE_MODE=capture

# Intervalo de captura (ms)
CAPTURE_INTERVAL=1500

# Debug mode
DEBUG=false
